export enum SoundErrors {
  INVALID_FILE = "File doesn't exists",
  INVALID_VOLUME = 'Invalid value to volume, it should be between 0 and 1',
  PAUSE_ERROR = 'Could not pause the sound',
}

// export const CLIErrors = {};
